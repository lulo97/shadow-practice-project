Standard stream = connections between computers and it's environment when execute commands

Three type of connection:
- stdin = stream of data from input
- stdout = stream of output data
- stderr = stream of error logs, info logs...